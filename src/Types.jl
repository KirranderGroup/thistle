module Types

"""
Numeric aliases used throughout the Julia tools.

* `DP` - default floating-point precision (Float64)
* `IK` - default integer kind (Int64)
"""
const DP = Float64
const IK = Int64

export DP, IK

end
